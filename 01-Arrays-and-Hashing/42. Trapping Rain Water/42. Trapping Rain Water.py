                left_max = max(left_max, height[left])                water += left_max - height[left]        while left < right:            if height[left] < height[right]:                left += 1            else:        water = 0        right_max = 0                right_max = max(right_max, height[right])                water += right_max - height[right]                right -= 1        return water